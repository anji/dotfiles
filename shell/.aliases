# ============================================================================
# Shell Aliases - Common Productivity Shortcuts
# ============================================================================

# ============================================================================
# Basic Utilities (with modern replacements)
# ============================================================================

# ls replacements with exa (if available)
if command -v exa &> /dev/null; then
    alias ls='exa --icons'
    alias ll='exa -lah --icons --git'
    alias la='exa -a --icons'
    alias lt='exa --tree --level=2 --icons'
    alias lta='exa --tree --level=2 --icons -a'
else
    alias ll='ls -lah'
    alias la='ls -A'
    alias l='ls -CF'
fi

# cat replacement with bat (if available)
if command -v bat &> /dev/null; then
    alias cat='bat --style=auto'
    alias catp='bat --style=plain'
fi

# find replacement with fd (if available)
if command -v fd &> /dev/null; then
    alias find='fd'
fi

# grep replacement with ripgrep (if available)
if command -v rg &> /dev/null; then
    alias grep='rg'
fi

# ============================================================================
# Navigation
# ============================================================================

alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

# Quick directory access
alias ~='cd ~'
alias -- -='cd -'

# ============================================================================
# File Operations
# ============================================================================

alias cp='cp -iv'
alias mv='mv -iv'
alias rm='rm -iv'
alias mkdir='mkdir -pv'

# Safer alternatives
alias cpv='rsync -ah --info=progress2'

# ============================================================================
# Git Aliases (Additional to .gitconfig)
# ============================================================================

alias g='git'
alias ga='git add'
alias gaa='git add .'
alias gap='git add -p'
alias gb='git branch'
alias gba='git branch -a'
alias gbd='git branch -d'
alias gbD='git branch -D'
alias gc='git commit'
alias gcm='git commit -m'
alias gca='git commit --amend'
alias gcane='git commit --amend --no-edit'
alias gco='git checkout'
alias gcob='git checkout -b'
alias gd='git diff'
alias gds='git diff --staged'
alias gf='git fetch'
alias gl='git log --oneline --decorate --graph'
alias gll='git log --oneline --decorate --graph --all'
alias gp='git push'
alias gpf='git push --force-with-lease'
alias gpl='git pull'
alias gpr='git pull --rebase'
alias gr='git rebase'
alias gri='git rebase -i'
alias gs='git status'
alias gss='git status -s'
alias gst='git stash'
alias gstp='git stash pop'
alias gsta='git stash apply'
alias gstl='git stash list'
alias gsw='git switch'
alias gswc='git switch -c'
alias gwip='git add -A && git commit -m "WIP"'

# GitHub CLI shortcuts (if installed)
if command -v gh &> /dev/null; then
    alias ghpr='gh pr create'
    alias ghprs='gh pr status'
    alias ghprv='gh pr view'
    alias ghrc='gh repo clone'
    alias ghrv='gh repo view'
fi

# ============================================================================
# Docker Aliases
# ============================================================================

alias d='docker'
alias dc='docker-compose'
alias dcu='docker-compose up'
alias dcud='docker-compose up -d'
alias dcd='docker-compose down'
alias dcl='docker-compose logs'
alias dclf='docker-compose logs -f'
alias dcr='docker-compose restart'
alias dce='docker-compose exec'
alias dcb='docker-compose build'
alias dcp='docker-compose ps'

# Docker container management
alias dps='docker ps'
alias dpsa='docker ps -a'
alias di='docker images'
alias drm='docker rm'
alias drmi='docker rmi'
alias dstart='docker start'
alias dstop='docker stop'
alias drestart='docker restart'
alias dexec='docker exec -it'
alias dlogs='docker logs'
alias dlogsf='docker logs -f'
alias dinspect='docker inspect'

# Docker system commands
alias dprune='docker system prune -af'
alias dprune-volumes='docker system prune -af --volumes'
alias dvprune='docker volume prune -f'
alias dnprune='docker network prune -f'

# Docker network
alias dnet='docker network ls'
alias dnetinspect='docker network inspect'

# Docker volume
alias dvol='docker volume ls'
alias dvolinspect='docker volume inspect'

# Docker cleanup
alias docker-clean='docker rm $(docker ps -a -q) && docker rmi $(docker images -q)'
alias docker-clean-containers='docker rm $(docker ps -a -q)'
alias docker-clean-images='docker rmi $(docker images -q -f dangling=true)'

# ============================================================================
# Kubernetes Aliases
# ============================================================================

if command -v kubectl &> /dev/null; then
    alias k='kubectl'
    alias kg='kubectl get'
    alias kd='kubectl describe'
    alias kdel='kubectl delete'
    alias kaf='kubectl apply -f'
    alias kdf='kubectl delete -f'
    alias kgp='kubectl get pods'
    alias kgs='kubectl get services'
    alias kgd='kubectl get deployments'
    alias kgn='kubectl get nodes'
    alias kgns='kubectl get namespaces'
    alias kl='kubectl logs'
    alias klf='kubectl logs -f'
    alias ke='kubectl exec -it'
    alias kpf='kubectl port-forward'
    alias kctx='kubectl config current-context'
    alias kns='kubectl config set-context --current --namespace'
fi

# ============================================================================
# System Monitoring
# ============================================================================

# Process management
alias psa='ps aux'
alias psg='ps aux | grep -v grep | grep -i -e VSZ -e'
alias topcpu='ps aux | sort -nk 3,3 | tail -n 10'
alias topmem='ps aux | sort -nk 4,4 | tail -n 10'

# Disk usage
alias df='df -h'
alias du='du -h'
alias dus='du -sh'
alias ducks='du -cks * | sort -rn | head'

# Network
alias ports='netstat -tulanp'
alias listening='lsof -i -P | grep LISTEN'

# System info
alias myip='curl -s ifconfig.me'
alias localip="ip addr show | grep 'inet ' | grep -v '127.0.0.1' | awk '{print \$2}' | cut -d/ -f1"

# ============================================================================
# Development
# ============================================================================

# Python
alias py='python3'
alias python='python3'
alias pip='pip3'
alias venv='python3 -m venv'
alias activate='source venv/bin/activate'

# Node.js
alias ni='npm install'
alias nid='npm install --save-dev'
alias nig='npm install -g'
alias nis='npm install --save'
alias nr='npm run'
alias nrs='npm run start'
alias nrb='npm run build'
alias nrt='npm run test'
alias nrd='npm run dev'
alias nls='npm list'

# Yarn
alias y='yarn'
alias ya='yarn add'
alias yad='yarn add --dev'
alias yr='yarn remove'
alias ys='yarn start'
alias yb='yarn build'
alias yt='yarn test'
alias yd='yarn dev'

# ============================================================================
# Editor Shortcuts
# ============================================================================

alias vim='nvim'
alias vi='nvim'
alias v='nvim'

# Quick config edits
alias zshconfig='nvim ~/.zshrc'
alias vimconfig='nvim ~/.config/nvim/lua/custom/chadrc.lua'
alias aliasconfig='nvim ~/.aliases'
alias gitconfig='nvim ~/.gitconfig'

# ============================================================================
# Zellij/Terminal
# ============================================================================

alias zj='zellij'
alias zja='zellij attach'
alias zjl='zellij list-sessions'
alias zjk='zellij kill-session'
alias zjks='zellij kill-all-sessions'

# ============================================================================
# Utilities
# ============================================================================

# Time
alias now='date +"%T"'
alias nowdate='date +"%Y-%m-%d"'
alias timestamp='date +"%Y%m%d_%H%M%S"'

# History
alias h='history'
alias hg='history | grep'

# Reload shell
alias reload='source ~/.zshrc'

# Clear screen
alias c='clear'
alias cls='clear'

# Make executable
alias mkx='chmod +x'

# Create parent directories automatically
alias mkdir='mkdir -pv'

# Colorize output
alias diff='diff --color=auto'

# Weather
alias weather='curl wttr.in'

# Cheat sheets (if cheat is installed)
if command -v cheat &> /dev/null; then
    alias ch='cheat'
fi

# TLDR (if installed)
if command -v tldr &> /dev/null; then
    alias tl='tldr'
fi

# ============================================================================
# Safety Features
# ============================================================================

# Confirm before overwriting
alias mv='mv -i'
alias cp='cp -i'
alias ln='ln -i'

# Prevent accidental deletions
alias rm='rm -I --preserve-root'
alias chown='chown --preserve-root'
alias chmod='chmod --preserve-root'
alias chgrp='chgrp --preserve-root'

# ============================================================================
# Fun Stuff
# ============================================================================

# Matrix effect (if cmatrix is installed)
if command -v cmatrix &> /dev/null; then
    alias matrix='cmatrix -bs'
fi

# System information (if neofetch is installed)
if command -v neofetch &> /dev/null; then
    alias sysinfo='neofetch'
fi

# ============================================================================
# Terraform
# ============================================================================

if command -v terraform &> /dev/null; then
    alias tf='terraform'
    alias tfi='terraform init'
    alias tfp='terraform plan'
    alias tfa='terraform apply'
    alias tfd='terraform destroy'
    alias tfv='terraform validate'
    alias tff='terraform fmt'
    alias tfw='terraform workspace'
    alias tfo='terraform output'
fi

# ============================================================================
# AWS CLI
# ============================================================================

if command -v aws &> /dev/null; then
    alias awswho='aws sts get-caller-identity'
    alias awsregion='aws configure get region'
fi

# ============================================================================
# Custom Functions (call from .functions if needed)
# ============================================================================

# Quick note taking
alias note='nvim ~/notes/$(date +%Y-%m-%d).md'
